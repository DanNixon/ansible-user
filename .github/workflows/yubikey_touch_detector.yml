---
name: "dannixon.user.yubikey_touch_detector"

on:
  push:
    paths:
      - 'roles/yubikey_touch_detector/**'
      - '.github/workflows/yubikey_touch_detector.yml'
  pull_request:
    paths:
      - 'roles/yubikey_touch_detector/**'
      - '.github/workflows/yubikey_touch_detector.yml'

jobs:
  molecule:
    runs-on: ubuntu-latest
    env:
      PY_COLORS: 1
      ANSIBLE_FORCE_COLOR: 1
    steps:
      - name: Check out code
        uses: actions/checkout@v1

      - name: Set up Python
        uses: actions/setup-python@v1
        with:
          python-version: 3.8

      - uses: dannixon/ansible-testing/actions/test_role@main
        with:
          role: yubikey_touch_detector
